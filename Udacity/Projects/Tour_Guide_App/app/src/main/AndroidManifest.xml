<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.tourguideapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WeatherActivity" android:parentActivityName=".MainActivity" android:label="@string/category_weather"></activity>
        <activity android:name=".RestaurantsActivity" android:parentActivityName=".MainActivity" android:label="@string/category_restaurants"/>
        <activity android:name=".ThingsToDoActivity" android:parentActivityName=".MainActivity" android:label="@string/category_thingsToDo"/>
        <activity android:name=".AboutActivity" android:parentActivityName=".MainActivity" android:label="@string/category_about"/>
        <activity android:name=".MainActivity" android:parentActivityName=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>